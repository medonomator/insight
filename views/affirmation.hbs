<!DOCTYPE html>
<html lang="ru">

<head>
  {{>head}}
</head>


<body>
  <div class="bg"></div>
  <div class="wrapper">
    {{>header}}

    <h1>
      {{#if affirmationPage.headerH1}}
      {{affirmationPage.headerH1}}
      {{else}}
      В разработке...
      {{/if}}


      <div class="header-text">
        <p>
          {{#if affirmationPage.headerText}}
          {{affirmationPage.headerText}}
          {{else}}
          {{/if}}
      </div>
  </div>
  {{>footer}}
</body>

</html>