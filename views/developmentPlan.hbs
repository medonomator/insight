<!DOCTYPE html>
<html lang="ru">

<head>
  <title>цитаты великих людей</title>
  {{>head}}
</head>

<body>
  <div class="bg"></div>
  <div class="wrapper">
    {{>header}}

    <h1>
      {{#if developmentPlanPage.headerH1}}
      {{developmentPlanPage.headerH1}}
      {{else}}
      План развития проекта
      {{/if}}
    </h1>

    <div class="header-text">
      <p>
        {{#if developmentPlanPage.headerText}}
        {{developmentPlanPage.headerText}}
        {{else}}
        Проект стремиться давать светлый и чистый настрой на каждый день.
        {{!-- На данном этапе основная задача это наращивание корпуса афоризмов, афирмаций и материалов только самых
        отборных
        и мощных. --}}
        {{/if}}
      </p>
    </div>
  </div>
  {{>footer}}
</body>

</html>